<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aria.test.headfirst">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".project_one.MainActivity" />
        <activity
            android:name=".project_two.MultipleActivity"
            android:label="@string/title_activity_multiple"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".project_three.StopWatchActivity"
            android:label="@string/title_activity_stop_watch"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HandlerDemo"
            android:label="@string/title_activity_handler_demo"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".project_four.WorkoutActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>